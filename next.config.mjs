/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'media1.giphy.com',
            'i.ibb.co',
            'avatars.githubusercontent.com'
        ],
    },
};

export default nextConfig;
